spring:
  application:
    name: registration-service
server:
  port: 8084
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true

security:
#  user:
#    name: user
#    password: 123123
  basic:
    enabled: false